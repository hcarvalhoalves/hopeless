uses lines,string,words;

dec getc : list(char);
--- getc <= read("con");

dec gets : list(string);
--- gets <= lines(getc);

dec dir : string -> list(string);
--- dir(s) <= words(read("!dir /w " <> s));
